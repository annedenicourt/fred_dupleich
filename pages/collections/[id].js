import Head from 'next/head'
import Image from 'next/image'
import styles from '/Users/annedenicourt/Desktop/REACT/frederique-dupleich/styles/Home.module.css'
import NavBar from '../../components/NavBar'
import Card from 'react-bootstrap/Card'
import Footer from '../../components/Footer'
import Carousel from '../../components/Carousel'
import { categories } from '../../data/dataList'
import { useRouter } from "next/router";

export default function Collection(props) {

    const router = useRouter();
    const { id } = router.query; // Destructuring our router object

    const filter = categories.filter(category => category.name === id);
    console.log(filter)    


  return (
    <div className="">
        <Head>
            <title>Frédérique Dupleich Céramiste - Actualités</title>
            <meta name="description" content="Generated by create next app" />
            <link rel="icon" href="/favicon.ico" />
        </Head>
        <main>
            <div className={`${styles.my_container2} container px-0 mx-auto`}>            
                <div className="row mx-5 justify-content-center">
                    <NavBar/>
                </div>
                <div className="row mx-0 mt-5 pt-4 pb-5 bg-light justify-content-center">
                    <div className="col-9">
                        <div className={`mb-3`}>
                            <div className={`${styles.my_font} fs-4 me-3`}>Collections</div>
                            <div className={`fs-4`}>{id}</div>
                        </div>
                        <Carousel
                            name={filter[0].name}
                            id={filter[0].id}
                            images={filter[0].gallery}
                            image= {filter[0].image}
                        />
                    </div>
                    <div className="text-center mt-5">
                        <button className="border rounded-pill p-2"><a className="text-reset" href="/galerie"><i className="bi bi-arrow-left-short me-2"></i>Revenir à la galerie</a></button>            
                    </div> 
                </div>
                
            </div>
            <Footer/>
        </main>
    </div>
  )
}

